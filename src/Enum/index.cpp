#ifndef ENUM_HEADER
#define ENUM_HEADER
#include<iostream>

using namespace std;

enum ETipoPago { PAYPAL, TARJETA };

enum EEstado { FINALIZADA, ENCURSO };

enum ETipoPlataforma { XBOXONE, XBOXX, PS5, PC, SWITCH, PS4 };

enum EGeneroJuego { ACCCION, AVENTURA, ESTRATEGIA, DEPORTE };

enum EPeriodo { MENSUAL, TRIMESTRAL, ANUAL, VITALICIA };

#endif

